import { TransitionEndDirective } from './transition-end.directive';

describe('TransitionEndDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionEndDirective();
    expect(directive).toBeTruthy();
  });
});
