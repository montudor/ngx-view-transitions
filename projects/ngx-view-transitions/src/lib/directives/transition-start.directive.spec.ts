import { TransitionStartDirective } from './transition-start.directive';

describe('TransitionStartDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionStartDirective();
    expect(directive).toBeTruthy();
  });
});
