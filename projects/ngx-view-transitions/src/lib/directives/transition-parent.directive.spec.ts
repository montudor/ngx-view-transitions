import { TransitionParentDirective } from './transition-parent.directive';

describe('TransitionParentDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionParentDirective();
    expect(directive).toBeTruthy();
  });
});
